/* =========================================================
   config.js —— 站点配置
   想改品牌名 / 主色 / 默认预算 / 提醒时间，只改这一个文件。
   ========================================================= */
window.APP_CONFIG = {
  brand: {
    name: "BYJ 的个人看板",   // 侧边栏品牌名 + 浏览器标题
    sub: "Finance & Health",  // 品牌副标题
    logo: "B",                // 字母 LOGO（不用图片）
  },
  // 主色（同时改 css/style.css 里的 --accent 即可全局换色）
  accent: "#5c7cfa",
  budget: {
    defaultMonthly: 3000,     // 预算默认值（元），可在「设置」里按月修改
  },
  reminder: {
    healthHour: 20,           // 超过这个钟点还没打卡，打开网站时提醒
    monthlyDay: 2,            // 过了每月这天还没导入账单，打开网站时提醒
  },
  version: "1.0.0",
};
